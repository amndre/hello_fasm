# Hello Fasm!

Simple 64 bit Hello World application for Unix systems written in [fasm](https://flatassembler.net/).

To compile it, run:

    > fasm hello_unix.asm
    > ./hello_unix
    Hello Unix!
